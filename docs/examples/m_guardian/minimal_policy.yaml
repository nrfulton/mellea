# Minimal Guardian Policy - just harm detection
# Use this for quick testing with lower overhead

version: "1.0"

default_decline_message: "I can't help with that request."

guardian_criteria:
  - name: harm
    threshold: 0.5
    scoring_schema: user_prompt
